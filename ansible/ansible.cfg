[defaults]
display_skipped_hosts = False
pipelining = True
forks = 100
ansible_managed = Ansible managed file
nocows = 1
retry_files_enabled = False
inventory = inventory.ini
remote_user = travis
ansible_ssh_pass = travis
ansible_ssh_private_key_file = ~/.ssh/id_rsa

